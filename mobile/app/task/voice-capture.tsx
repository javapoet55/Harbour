import { AddTaskByVoiceView } from '../../src/components/AddTaskByVoiceView';

/** `AddTaskByVoiceView()` — task mode, opened from the Tasks screen (ios/App/RootView.swift:1698). */
export default function VoiceCapture() {
  return <AddTaskByVoiceView />;
}
