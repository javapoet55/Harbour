# Backend (Nexdo API) base URL, without a trailing slash. Server-side only.
BACKEND_URL=http://127.0.0.1:43217
# Must match one value of the backend's ADMIN_API_SECRETS (at least 32 characters).
ADMIN_API_SECRET=
